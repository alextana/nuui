import { Meta, Canvas, Story, Controls, ArgTypes } from '@storybook/addon-docs/blocks';
import Text from './Text.vue';
import * as TextStories from './Text.stories';

<Meta title="Docs/Text" of={TextStories} />

# Text Component

A comprehensive, customizable text component built with Vue 3 and Tailwind CSS. This component supports multiple semantic HTML elements, typography variants, tones, and alignment options to cover all your text rendering needs.

## Features

- 🏷️ **29 Semantic Elements**: h1-h6, p, span, div, label, strong, em, and more
- 📝 **11 Typography Variants**: Display, Heading, Body, Caption, and Label sizes
- 🎨 **9 Tones**: Default, Muted, Subtle, Success, Warning, Danger, Info, Brand, Accent
- 📐 **4 Alignments**: Left, Center, Right, Justify
- 🔤 **4 Font Weights**: Normal, Medium, Semibold, Bold
- ♿ **Accessibility**: Proper semantic HTML with screen reader support
- 🎛️ **Customizable**: Tailwind classes with easy theme overrides

## Basic Usage

<Canvas of={TextStories.Default} />

---

## Semantic Elements

Choose from 29 different HTML elements for proper semantic structure:

### Headings
<Canvas of={TextStories.Headings} source={{ code: `<Text as="h1" variant="heading3xl">Heading 3XL</Text>
<Text as="h2" variant="heading2xl">Heading 2XL</Text>
<Text as="h3" variant="headingXl">Heading XL</Text>` }} />

### Semantic Elements
<Canvas of={TextStories.SemanticElements} source={{ code: `<Text as="strong">Strong text</Text>
<Text as="em">Emphasized text</Text>
<Text as="code">Code text</Text>` }} />

---

## Typography Variants

Multiple typography variants for comprehensive text hierarchy:

### Body Text
<Canvas of={TextStories.BodyText} source={{ code: `<Text variant="bodyLg">Body Large</Text>
<Text variant="bodyMd">Body Medium</Text>
<Text variant="bodySm">Body Small</Text>
<Text variant="bodyXs">Body XS</Text>` }} />

---

## Tones

Different tones to convey meaning and hierarchy:

<Canvas of={TextStories.Tones} source={{ code: `<Text tone="base">Base tone</Text>
<Text tone="subdued">Subdued tone</Text>
<Text tone="success">Success tone</Text>
<Text tone="critical">Critical tone</Text>
<Text tone="caution">Caution tone</Text>` }} />

---

## Alignment

Four alignment options for text positioning:

<Canvas of={TextStories.Alignments} source={{ code: `<Text alignment="start">Left aligned text</Text>
<Text alignment="center">Center aligned text</Text>
<Text alignment="end">Right aligned text</Text>
<Text alignment="justify">Justified text</Text>` }} />

---

## Font Weights

Four font weight options for emphasis:

<Canvas of={TextStories.FontWeights} source={{ code: `<Text fontWeight="regular">Regular weight</Text>
<Text fontWeight="medium">Medium weight</Text>
<Text fontWeight="semibold">Semibold weight</Text>
<Text fontWeight="bold">Bold weight</Text>` }} />

---

## Utilities

Special text utilities for enhanced functionality:

<Canvas of={TextStories.Utilities} source={{ code: `<Text breakWord>Very long word that breaks</Text>
<Text truncate>Text that gets truncated</Text>
<Text numeric>1234567890</Text>
<Text textDecorationLine="line-through">Strikethrough</Text>` }} />

---

## Theme Customization

The Text component supports comprehensive theme customization through the `theme` prop. You can override any part of the text styling by providing a custom theme object.

### Theme Structure

The theme object is organized into sections:

- **`base`**: Base text styling classes
- **`variants`**: Typography variant classes (displayLg, headingMd, bodyMd, etc.)
- **`tones`**: Color and tone classes (default, muted, success, etc.)
- **`alignments`**: Text alignment classes (left, center, right, justify)
- **`weights`**: Font weight classes (normal, medium, semibold, bold)

### Custom Theme Example
<Canvas of={TextStories.CustomTheme} source={{ code: `<Text 
  variant="headingLg"
  :theme="{
    root: {
      variants: {
        headingLg: 'text-2xl font-black text-purple-600'
      }
    }
  }"
>
  Custom Theme Text
</Text>` }} />

### TypeScript Support

```typescript
import type { TextTheme } from './theme'

const customTheme: TextTheme = {
  base: 'font-mono tracking-wider',
  variants: {
    bodyMd: 'text-lg leading-relaxed'
  },
  tones: {
    accent: 'text-emerald-500 font-semibold'
  }
}
```

---

## Props API

<ArgTypes of={TextStories} />