import { Meta, Canvas, Story, Controls, ArgTypes } from '@storybook/addon-docs/blocks';
import Button from './Button.vue';
import * as ButtonStories from './Button.stories';

<Meta title="Docs/Button" of={ButtonStories} />

# Button Component

A comprehensive, customizable button component built with Vue 3 and Tailwind CSS. This component supports multiple variants, sizes, icons, and states to cover all your UI needs.

## Features

- 🎨 **6 Variants**: Primary, Secondary, Tertiary, Danger, Success, Warning
- 📏 **5 Sizes**: XS, SM, MD, LG, XL
- 🎯 **Icon Support**: Both prop-based and slot-based icons
- 🔄 **Loading States**: Built-in spinner with proper positioning
- ♿ **Accessibility**: Full keyboard navigation and screen reader support
- 🎛️ **Customizable**: Tailwind classes with easy overrides

## Basic Usage

<Canvas of={ButtonStories.Default} />

---

## Variants

Choose from 6 different button variants to match your design system:

### Primary
<Canvas of={ButtonStories.Primary} source={{ code: `<Button variant="primary" label="Primary Button" />` }} />

### Secondary
<Canvas of={ButtonStories.Secondary} source={{ code: `<Button variant="secondary" label="Secondary Button" />` }} />

### Tertiary
<Canvas of={ButtonStories.Tertiary} source={{ code: `<Button variant="tertiary" label="Tertiary Button" />` }} />

### Danger
<Canvas of={ButtonStories.Danger} source={{ code: `<Button variant="danger" label="Danger Button" />` }} />

### Success
<Canvas of={ButtonStories.Success} source={{ code: `<Button variant="success" label="Success Button" />` }} />

### Warning
<Canvas of={ButtonStories.Warning} source={{ code: `<Button variant="warning" label="Warning Button" />` }} />

---

## Sizes

Five different sizes to fit any layout:

### Extra Small
<Canvas of={ButtonStories.ExtraSmall} source={{ code: `<Button size="xs" label="Extra Small" />` }} />

### Small
<Canvas of={ButtonStories.Small} source={{ code: `<Button size="sm" label="Small" />` }} />

### Medium
<Canvas of={ButtonStories.Medium} source={{ code: `<Button size="md" label="Medium" />` }} />

### Large
<Canvas of={ButtonStories.Large} source={{ code: `<Button size="lg" label="Large" />` }} />

### Extra Large
<Canvas of={ButtonStories.ExtraLarge} source={{ code: `<Button size="xl" label="Extra Large" />` }} />

---

## Icons

### Prop-based Icons

Pass icon components directly as props:

#### Left Icon
<Canvas of={ButtonStories.WithLeftIcon} source={{ code: `<Button variant="primary" label="Add Item" :left-icon="PlusIcon" />` }} />

#### Right Icon
<Canvas of={ButtonStories.WithRightIcon} source={{ code: `<Button variant="secondary" label="Download" :right-icon="DownloadIcon" />` }} />

#### Both Icons
<Canvas of={ButtonStories.WithBothIcons} source={{ code: `<Button variant="tertiary" label="Search" :left-icon="SearchIcon" :right-icon="DownloadIcon" />` }} />

### Slot-based Icons

Use slots for maximum flexibility:

<Canvas of={ButtonStories.WithSlotIcons} source={{ code: `<Button variant="secondary" label="Slot Icons">\n  <template #iconLeft>\n    <PlusIcon />\n  </template>\n  <template #iconRight>\n    <SearchIcon />\n  </template>\n</Button>` }} />

### Icon-Only Buttons

Perfect for toolbars and compact layouts:

#### Standard Icon Only
<Canvas of={ButtonStories.IconOnly} source={{ code: `<Button variant="primary" :left-icon="HeartIcon" icon-only />` }} />

#### Rounded Icon Only
<Canvas of={ButtonStories.IconOnlyRounded} source={{ code: `<Button variant="secondary" :left-icon="PlusIcon" icon-only rounded="full" />` }} />

---

## States

### Disabled State
<Canvas of={ButtonStories.Disabled} source={{ code: `<Button variant="primary" label="Disabled Button" disabled />` }} />

### Loading State

#### Basic Loading
<Canvas of={ButtonStories.Loading} source={{ code: `<Button variant="primary" label="Loading..." loading />` }} />

#### Loading with Icon
<Canvas of={ButtonStories.LoadingWithIcon} source={{ code: `<Button variant="success" label="Submitting" :left-icon="PlusIcon" loading />` }} />

---

## Layout Options

### Full Width
<Canvas of={ButtonStories.FullWidth} source={{ code: `<Button variant="primary" label="Full Width Button" full-width />` }} />

### Rounded Variations

#### No Rounded
<Canvas of={ButtonStories.RoundedNone} source={{ code: `<Button rounded="none" label="No Rounded" />` }} />

#### Small Rounded
<Canvas of={ButtonStories.RoundedSmall} source={{ code: `<Button rounded="sm" label="Small Rounded" />` }} />

#### Large Rounded
<Canvas of={ButtonStories.RoundedLarge} source={{ code: `<Button rounded="lg" label="Large Rounded" />` }} />

#### Fully Rounded
<Canvas of={ButtonStories.RoundedFull} source={{ code: `<Button rounded="full" label="Fully Rounded" />` }} />

---

## Advanced Usage

### Custom Styling
<Canvas of={ButtonStories.CustomStyling} source={{ code: `<Button \n  variant="primary" \n  label="Custom Styled" \n  custom-classes="shadow-lg transform hover:scale-105 transition-transform" \n/>` }} />

---

## Theme Customization

The Button component supports comprehensive theme customization through the `theme` prop. You can override any part of the button's styling by providing a custom theme object.

### Theme Structure

The theme object is organized into sections:

- **`root`**: Main button element styling (base, variants, sizes, rounded)
- **`icon`**: Icon-specific styling (base classes, sizes)
- **`content`**: Content wrapper styling
- **`loading`**: Loading spinner styling

### Custom Gradient Theme
<Canvas of={ButtonStories.CustomTheme} source={{ code: `<Button \n  variant="primary" \n  label="Custom Theme" \n  :theme="{\n    root: {\n      base: 'inline-flex items-center justify-center font-bold transition-all duration-300 focus:outline-none focus:ring-4 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed relative transform hover:scale-105',\n      variants: {\n        primary: 'bg-gradient-to-r from-purple-500 to-pink-500 text-white hover:from-purple-600 hover:to-pink-600 focus:ring-purple-500 border border-transparent shadow-lg'\n      },\n      rounded: {\n        md: 'rounded-2xl'\n      }\n    },\n    icon: {\n      base: 'drop-shadow-sm'\n    },\n    content: {\n      base: 'tracking-wide'\n    }\n  }"\n/>` }} />

### Minimal Theme
<Canvas of={ButtonStories.MinimalTheme} source={{ code: `<Button \n  variant="primary" \n  label="Minimal Theme" \n  :theme="{\n    root: {\n      base: 'inline-flex items-center justify-center font-medium transition-colors duration-200 focus:outline-none disabled:opacity-50 disabled:cursor-not-allowed relative',\n      variants: {\n        primary: 'bg-black text-white hover:bg-gray-800 border border-black'\n      },\n      rounded: {\n        md: 'rounded-none'\n      }\n    }\n  }"\n/>` }} />

### TypeScript Support

```typescript
import type { ButtonTheme } from './theme'

const customTheme: ButtonTheme = {
  root: {
    variants: {
      primary: 'bg-gradient-to-r from-blue-500 to-purple-600 text-white'
    }
  },
  icon: {
    base: 'drop-shadow-lg'
  }
}
```

### Theme Examples Component
<Canvas of={ButtonStories.ThemeExamples} />

---

## Props API

<ArgTypes of={ButtonStories} />
